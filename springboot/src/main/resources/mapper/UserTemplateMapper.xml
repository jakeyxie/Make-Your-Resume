<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserTemplateMapper">
    <select id="selectAll" resultType="com.example.entity.UserTemplate">
        select usertemplate.*, resumesort.sort as resumeSort from `usertemplate`
        left join resumesort on usertemplate.resumeId = resumesort.resumeSortId
        <where>
            <if test="userId != null and userId != ''"  >and usertemplate.userId = #{userId}</if>
        </where>
        order by id desc
    </select>

    <insert id="insert">
        insert into `usertemplate` (img, content, time, resumeId, userId)
        values(#{img}, #{content}, #{time}, #{resumeId}, #{userId})
    </insert>

    <update id="updateById">
        update `usertemplate` set img = #{img}, content = #{content}, time = #{time}, resumeId = #{resumeId}, userId = #{userId}
        where id = #{id}
    </update>

</mapper>